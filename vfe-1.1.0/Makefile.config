#
# Common configuration infomations for all platforms
#
DRV	= vfe
VERSION	= 1.1.0
PKGREV	=
DISTDIR	= ../${DRV}-${VERSION}${PKGREV}

DFLAGS	= -DDEBUG -DDEBUG_LEVEL=0 -DGEM_DEBUG_LEVEL=0 -USANITY \
	  -UTXTIMEOUT_TEST -UTEST_TXDESC_FULL -UTEST_RX_EMPTY \
	  -UDEBUG_SUSPEND
#
# MAP_MEM doesn't work on sparc
#
CFGFLAGS = -DGEM_DELAYED_START -UOPT_TX_INTR -UMAP_MEM
