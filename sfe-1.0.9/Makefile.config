#
# Common configuration infomations for all platforms
#
DRV	= sfe
VERSION	= 1.0.9
PKGREV	=
DISTDIR	= ../${DRV}-${VERSION}${PKGREV}

DFLAGS	= -UDEBUG -DDEBUG_LEVEL=1 -UGEM_DEBUG_LEVEL

#
# MAP_MEM doesn't work for sis900
#
CFGFLAGS = -DCONFIG_POLLING -UMAP_MEM -DGEM_DELAYED_START \
	   -DCONFIG_DP83815 -DCONFIG_SIS900 -DCONFIG_SIS7016
