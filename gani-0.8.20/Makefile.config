#
# Common configuration infomations for all platforms
#
DRV	= gani
VERSION	= 0.8.20
PKGREV	=
DISTDIR	= ../${DRV}-${VERSION}${PKGREV}

DFLAGS	= -UDEBUG -DDEBUG_LEVEL=0 -UGEM_DEBUG_LEVEL -UTEST_TXDESC_FULL \
	  -UTEST_RX_EMPTY -UDEBUG_HANG -UNEW

CFGFLAGS = -DGEM_DELAYED_START -DTX_MAX_FRAGS=8 -DTX_RING_SIZE=256 \
	   -DGEM_CONFIG_POLLING -DCONFIG_POLLING \
	   -DWA_TX_DESC_NO_WRAP -DWA_TPQ_NO_WRAP \
	   -UGEM_CONFIG_VLAN -UCONFIG_VLAN \
	   -UGEM_CONFIG_CKSUM_OFFLOAD -UCONFIG_CKSUM_OFFLOAD 
